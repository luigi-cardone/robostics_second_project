include "models/map.inc"
include "models/turtlebot.inc"

# configure the GUI window
window
( 
	size [700.000 700.000] 
	scale 0.05
)


# load an environment bitmap
floorplan
(
	bitmap "../maps/slam_map.png" 
	# Width and hight in meters, already to stale with the map, res = 5 cm/px
	size [31.25 18.20 3]
)

turtlebot
(
  pose [ 0 0 0 0]
  name "turtlebot"
  color "black"
)